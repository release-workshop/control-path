//! Reusable operations for compile and SDK generation
//!
//! These functions are used by multiple commands to avoid code duplication
//! and ensure consistent behavior across the CLI.

pub mod compile;
pub mod generate_sdk;
