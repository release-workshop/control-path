# Dependencies
node_modules/
.pnp
.pnp.js
jspm_packages/

# Build outputs
dist/
build/
*.tsbuildinfo

# Test coverage
coverage/
.nyc_output/
*.lcov

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Parcel-bundler cache
.cache
.parcel-cache

# Turborepo
.turbo/

# Deno
.deno/
.deno-cache/
.deno-workaround/
deno.lock

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.vscode-test

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Lock files
# Keep pnpm-lock.yaml - it should be committed for reproducible builds
# Ignore npm and yarn lock files (we use pnpm)
package-lock.json
yarn.lock

# Project-specific: Control Path
# AST artifacts (compiled from deployments - these are generated)
*.ast

# Control Path project files
# Note: .controlpath/schemas/ should be committed if they're project-specific
# But deployment files, AST artifacts, and signing keys should not be committed
.controlpath/*.deployment.yaml
.controlpath/*.ast
.controlpath/*.pem
.controlpath/signing-key.pem
.controlpath/config.yaml

# Temporary files
*.tmp
*.temp
.tmp/
.temp/

# Rust
# Build output directory
target/
# Backup files from rustfmt
**/*.rs.bk
# Rust compiler info
.rustc_info.json
# Rust build artifacts
**/*.rlib
**/*.rmeta
**/*.pdb
# Note: Cargo.lock should be committed for binaries/apps (like the CLI)
# but can be ignored for libraries. Since this is a monorepo with a CLI,
# Cargo.lock is kept in the repository.

# Binary executables and test files
# Test binaries (e.g., test-native, packages/cli/test-native)
test-native
**/test-native
**/*-test-native
**/*-native
# Explicit path for known test binary
packages/cli/test-native
# Compiled binaries (should be in dist/ but adding explicit patterns)
**/*.exe
**/*.bin
**/*.so
**/*.dylib
**/*.dll
**/*.wasm
# Object files
**/*.o
**/*.a
