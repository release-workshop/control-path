{
  "name": "@controlpath/comparison-tests",
  "version": "0.1.0",
  "description": "Comparison tests for TypeScript and Rust compiler implementations",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@controlpath/compiler": "workspace:*",
    "msgpackr": "^1.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.3",
    "vitest": "^1.0.0"
  }
}

